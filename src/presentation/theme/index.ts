import { colors } from "./Colors";

const theme = {
  colors: colors,
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 40,
  },
  roundness: 8,
  // Burada fonts, metrics gibi diğer tema özelliklerini de ekleyebiliriz.
};

export default theme;
